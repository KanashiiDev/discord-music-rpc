const CONFIG = {
  serverPort: 3000,
  activeInterval: 5000,
  idleInterval: 10000,
  stuckThreshold: 20000,
  healthCheckInterval: 10000,
  maxRetries: 5,
  serverCacheTime: 4000,
  requestTimeout: 3000,
  debugMode: 0
};
